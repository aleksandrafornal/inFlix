import { LightningElement } from 'lwc';
import CUSTOM_FOOTER from '@salesforce/resourceUrl/CustomFooterImage';

export default class UploadPhoto extends LightningElement {
    imagePath = CUSTOM_FOOTER;
}